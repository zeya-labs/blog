```prompt
请自动完成环境配置，并遵循以下规则：

目标：
安装并配置以下工具：
- zoxide
- atuin
- lazygit
- eza
- bat
- fd
- rg
- uv

要求：
1. 必须使用各项目 Git 仓库 / 官方文档当前推荐的安装方式（不要自行选择第三方教程）。
2. 优先安装“官方推荐的稳定版本（stable / latest recommended）”，不要安装 nightly、beta、dev 或过时版本。
3. 安装前先检测当前系统环境（操作系统、发行版、包管理器、Shell）。
4. 优先使用系统包管理器；若官方明确推荐其他方式，则按官方方式执行。
5. 所有操作必须幂等：
   - 已安装则跳过
   - 已存在配置不要重复写入
   - 更新配置前自动备份
1. 自动完成 Shell 集成（bash/zsh/fish，按当前环境判断）
2. 修改配置后验证是否生效：
   - 输出版本号
   - 验证命令可执行
   - 验证 shell 启动无报错
8. 输出：
   - 执行的命令
   - 修改的文件
   - 最终状态摘要
9. 如果存在多个官方推荐方案，请说明理由后选择最佳实践方案。
10. 除非必须，不要请求人工确认，直接执行。

最终目标：安装完成后打开新终端即可直接使用。
```